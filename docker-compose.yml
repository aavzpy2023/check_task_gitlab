services:

  nginx:
    build:
      context: ./nginx
    container_name: main_proxy
    ports:
      - "8503:80"
    networks:
      - app-network
    depends_on:
      - backend
      - frontend-react
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    container_name: gitlab_dashboard_backend
    volumes:
      - ./backend:/app
    env_file:
      - .env
    networks:
      - app-network
    depends_on:
      postgres_chatbot:
        condition: service_healthy
    restart: unless-stopped

  frontend-react:
    build:
      context: ./frontend-react
    container_name: gitlab_dashboard_frontend_dev
    volumes:
      - ./frontend-react:/app
      - /app/node_modules
    networks:
      - app-network
    restart: unless-stopped
    
  postgres_chatbot:
    image: postgres:16
    container_name: postgres_chatbot
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    # SSS: Aseguramos que la base de datos se una explícitamente a la red
    networks:
      - app-network
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

# SSS: Definición explícita de la red compartida
networks:
  app-network:
    driver: bridge

volumes:
  postgres_data: